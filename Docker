## UNCHECKED
FROM python:3-alpine

RUN apk add --no-cache build-base gcc musl-dev libffi-dev

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8000

## DEVMODE, in a production version everything is done through main.py and the MakeFile
## For now, UVICORN is in reload mode
CMD ["python", "/app/eiko/efastapi.py"]